galaxy_info:
  author: waal70 (Andre)
  description: Role to install pi-hole on a system. Will determine existence of samba-ad-dc and create dummy interface for pi-hole if needed.
    Please note that this role is now only working with PiHole version 6.0 and above.
  company: waal70
  platforms:
    - name: Debian
      versions:
        - bookworm
        - bullseye

  license: MIT

  min_ansible_version: "2.1"

  galaxy_tags: [debian,
                pihole
  ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
